import React from "react";
import { Layout } from "containers";

const MainPage = () => {
  return (
    <Layout isEmpty={true}>
      <span>데이터 있음</span>
    </Layout>
  );
};

export default MainPage;
